[tool.poetry]
name = "token-generator"
version = "v0.9.0"
description = ""
authors = ["Joao Pedro Watson <joaopedrowatson@hotmail.com>"]
readme = "README.md"
packages = [{include = "token_generator"}]

[tool.poetry.dependencies]
python = ">=3.8,<=3.11"
pillow = "^10.0.0"
typer = {extras = ["all"], version = "^0.9.0"}
rembg = "^2.0.50"
blue = "^0.9.1"
isort = "^5.12.0"

[tool.poetry.scripts]
generate-token = 'token_generator.main:app'

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
