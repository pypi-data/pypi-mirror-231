[tool.poetry]
name = "cisort"
version = "1.1.4"
description = "C/C++ include sort"
authors = ["Pavel <toruchan174@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.6"

[tool.poetry.scripts]
cisort = 'cisort:cisort'

[package]
include = ["src/*.py"]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"