from setuptools import setup, find_packages

setup(
    name='wrblito_selenium_wait',
    version='0.4',
    packages=find_packages(),
    install_requires=[
        'selenium',
        # inne zależności
    ],
    # inne metadane
)