[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "MT_PY_File_upload"
version = "0.2.2"
authors = [
  { name="Viraj", email="virajpoojari0905@gmail.com" },
]
description = "A package to implementing file upload in AWS s3 bucket and store mysqlite,msql,postgress"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.urls]
"Homepage" = "https://github.com/pypa/sampleproject"
"Bug Tracker" = "https://github.com/pypa/sampleproject/issues"