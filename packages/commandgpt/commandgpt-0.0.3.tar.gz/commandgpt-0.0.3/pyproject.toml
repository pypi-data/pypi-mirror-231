[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "commandgpt"
version = "0.0.3"
description = "A CLI utility for interacting with OpenAI GPT."
authors = ["Michael Knap <you_need_to_add_email_here@example.com>"]
readme = "README.md"
homepage = "https://github.com/michaelknap/commandgpt"
license = "MIT"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX :: Linux"
]

[tool.poetry.dependencies]
python = ">=3.6"
openai = "*"

[tool.poetry.scripts]
gpt = "commandgpt:main"
