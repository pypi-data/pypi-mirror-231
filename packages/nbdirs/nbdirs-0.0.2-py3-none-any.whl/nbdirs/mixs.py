# AUTOGENERATED! DO NOT EDIT! File to edit: ../nbs/04_mixs.ipynb.

# %% auto 0
__all__ = ['ROOT_MIXIN', 'roothook']

# %% ../nbs/04_mixs.ipynb 4
import os, copy, inspect
from string import Template
from types import MethodType
from typing import Any, Dict, List, Type, Union, Optional, Iterable
from dataclasses import dataclass, field, InitVar, Field
from enum import Enum, EnumMeta, _EnumDict, member

# %% ../nbs/04_mixs.ipynb 6
from .cons import (__RESTORE__, __MEMBERS__, __INHERITABLE_MEMBERS__, ROOT, DROOT)
from .dirs import RelDir

# %% ../nbs/04_mixs.ipynb 8
def roothook(val, **kwargs):
    root = kwargs.get('root', DROOT)
    # print(val.root, val.path, val.full, root)    
    if isinstance(val, RelDir):        
        if val.abrv == ROOT.upper(): 
            val = val.update(root=root, path='.', inplace=False)
        else:
            val = val.update(root=root, inplace=False)
    return val

ROOT_MIXIN = (RelDir, roothook)
