from setuptools import setup

setup(
    name='pyarrow-tests',
    version="1.0.0",
    description='pyarrow-tests - listed by Cycode to prevent confusion',
    long_description="pyarrow-tests",
    long_description_content_type='text/markdown',
)
