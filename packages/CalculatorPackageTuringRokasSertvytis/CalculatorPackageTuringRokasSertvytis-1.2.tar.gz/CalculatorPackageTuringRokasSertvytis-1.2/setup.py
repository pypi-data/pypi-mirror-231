from setuptools import setup, find_packages

setup(
    name="Calculator",
    version = "1.0",
    packages = find_packages(),
    install_requires = [],
    description = "Calculator package to perform addition, subtraction, multiplication, division and take root of n"
)
