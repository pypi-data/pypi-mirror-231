[tool.poetry]
name = "easyeda-python-sdk"
version = "0.1.0"
description = "Python SDK for easyeda"
authors = ["Thomas Mahé <contact@tmahe.dev>"]
license = "MIT"
readme = "README.md"
packages = [{include = "easyeda"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
