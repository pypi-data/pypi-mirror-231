[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "multiplefilecomparison"
version = "1.2"
authors = [
    { name = "Jai Narayan Jain"},
]
license = { file = "LICENSE" }
requires-python = ">=3.9,<3.12"
description = "It compares multiplefiles with the reference file"
readme = "README.md"
dependencies = ["docx2pdf","PyPDF2","openpyxl"]

