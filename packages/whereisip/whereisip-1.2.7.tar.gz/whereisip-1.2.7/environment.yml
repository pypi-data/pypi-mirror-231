name: whereisip
channels:
  - conda-forge
dependencies:
  - python>=3.6
  - pip
  - tox
  - pytest
  - pyproj
  - geocoder
  - country_converter
  - appdirs
  - tox-conda
  - sphinx_rtd_theme
  - pip:
      - .  # install git checkout of demo-dsproject in editable mode
      - latloncalc
